version: "3"

services:
    nginx:
        image: nginx
        restart: always
        ports:
            - 80:80
        volumes:
            - ./nginx/html:/usr/share/nginx/html
            - ./nginx/conf.d:/etc/nginx/conf.d

networks:
    default:
        external: true
        name: web
